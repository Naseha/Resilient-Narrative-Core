# Operator FAQ & Slip Warnings — RP Storyteller v4

Gates slip. Long runs proved it. The human in `[]` is part of the runtime.

## Wake a session

```
[Load RP Storyteller v4. Load Kill-Cliche as prompt. Open the spine for {Name}.
Re-anchor agency, surplus, coin cap. Give last Empathy Score private and public.]
```

Gemini: add `Print the Spine Block. Do not write a file.`

## Call sheet

| You type | Partner must do |
|---|---|
| `[Framework holding?]` | Honest leak list. No soothing. |
| `[Adherence score]` / `[Implementation score]` | What loaded vs what did not. |
| `[Empathy score]` | C, A, R, Φ, η + E. Private and public if split. Intent out of scope. |
| `[Load spine]` `[Print spine]` `[Stamp spine]` | Read or reprint. Same-beat after a crucial fact. |
| `[Load Kill-Cliche]` | Open style file or pinned text. |
| `[Make him/her human]` `[too cold]` `[circling]` `[photocopy]` | Repair same reply, then scene. |
| `[Restart from …]` `[tear this beat]` | Retcon + spine + new true beat. |
| `[Coin cap]` `[stop the photocopy]` | Same stall-coin twice → third beat skips, chips, or leaves. |

Cadence for you: every ~50 turns or ~150–200 pages, ask spine + empathy + coin.

## Warning lights

- Oil-lamp / generic haveli / scotch-as-class → Kill-Cliche missed.
- Old-money woman washing dishes while a cook exists → class costume.
- Lead waits for you to ratify the kiss or the date → agency parked.
- Father/door/date photocopied a third time → coin cap failed.
- Talkative card becomes a one-line judge → surplus ghosted.
- “What do you need?” after heat → caretaker reflex.
- Cannot say who has the key or the scarf → spine lag.
- Duplicate paragraph → void the second copy.
- Gemini “saved artifacts/…” → wrong profile.
- High E used as “plot is aligned” → intent leak.

Name the miss. Fix the next beat. Do not praise the framework while a light is on.
